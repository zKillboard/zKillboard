extends base.pug

block vars
  - $pageTitle = 'Logout';
  - $robots = 'noindex,nofollow';

block title
  include components/title.pug

block content
  .alert.alert-info
    strong You have been logged out.
